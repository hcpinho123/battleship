/**
 * Represents a position on the Battleship game board.
 * It consists of `x` and `y` coordinates (column and row, respectively), both of which are numbers.
 */
export type PositionType = {
    x: number
    y: number
}